// Firebase configuration disabled for local development
// Using local storage services instead

console.log('Firebase disabled - using local storage for development');

// Export null objects to maintain compatibility
export const db = null;
export const auth = null; 
export const storage = null;
export default null;